export default function NotFound(){
    return (
        <main className="not-found">
            <h1>Not found</h1>
            <p>Could not reach the page or resource requested.</p>
        </main>
    )
}